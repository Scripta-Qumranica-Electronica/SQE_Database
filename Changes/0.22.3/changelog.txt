Removes artefacts that have an artefact_data but no artefact_shape.  It is unclear when these were created, but they do not belong in the system.

Add the possibility of an arteact_shape that has a link to an sqe_image_id, but does not have a shape set.